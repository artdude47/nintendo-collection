@page "/"
@inject NavigationManager Nav

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            // replace avoids polluting history; no need for forceLoad
            Nav.NavigateTo("/items", replace: true);
        }
    }
}
